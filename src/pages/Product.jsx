import React from "react";
import { Typography, Box } from "@mui/material";

const Product = () => (
  <Box sx={{ padding: 4 }}>
    <Typography variant="h4">Welcome to the Product Page</Typography>
  </Box>
);

export default Product;
