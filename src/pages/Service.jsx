import React from "react";
import { Typography, Box } from "@mui/material";

const Service = () => (
  <Box sx={{ padding: 4 }}>
    <Typography variant="h4">Welcome to the Service Page</Typography>
  </Box>
);

export default Service;
